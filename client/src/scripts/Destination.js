/**
 * Destination object created by performing a search through the searchbar. 
 * This object will store the name, long, lat, and other key values.
 * 
 */
export default class Destination {
    name = '';
    longitude = '';
    latitude = '';
    searchQuery = '';
}